language: php

php:
  - 7.0

before_install:
  - curl -sS https://getcomposer.org/installer | php
  - php composer.phar install

script: bin/phpunit --configuration ./tests/unit/phpunit.xml ./tests/unit
