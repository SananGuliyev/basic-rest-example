language: php

php:
  - 7.0

before_install:
  - curl -sS https://getcomposer.org/installer | php
  - php composer.phar install

script: ./vendor/phpunit/phpunit/phpunit --configuration ./tests/unittests/phpunit.xml ./tests/unit
